{
  "name": "guideforge",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build": "turbo run build",
    "check": "turbo run check",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "boundary": "node scripts/check-boundaries.mjs",
    "dep-check": "node scripts/check-deps.mjs",
    "security:audit": "node scripts/check-audit-policy.mjs",
    "security:licenses": "node scripts/check-license-policy.mjs",
    "security:sbom": "env -u npm_execpath -u npm_config_user_agent cyclonedx-npm --ignore-npm-errors --output-file sbom.xml",
    "security:secret-scan": "bash scripts/secret-scan.sh",
    "security:policy-test": "node scripts/policy-scripts.test.mjs",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo run build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "2.31.1",
    "@eslint/js": "10.0.1",
    "eslint": "10.8.0",
    "eslint-plugin-react-hooks": "7.1.1",
    "eslint-plugin-react-refresh": "0.4.20",
    "prettier": "3.9.6",
    "prettier-plugin-organize-imports": "4.3.0",
    "turbo": "2.10.8",
    "typescript": "6.0.3",
    "typescript-eslint": "8.66.0",
    "@cyclonedx/cyclonedx-npm": "catalog:"
  }
}
